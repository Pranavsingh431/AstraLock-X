# `core/perception`

Extracts `TargetObservation`s from a `CameraSensorFrame`: thresholding,
centroiding, matched filtering, and whatever else a detector needs.

Unprivileged. Sees pixels, never target positions.

_Arrives in Phase 3. Empty by design at Phase 0._
